{% extends 'base.html.twig' %}

{% block title %}Hello PathController!{% endblock %}
{% set string= " some string \n with backslash `\n  read escape filter" %}

{% block body %}


<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>
    <ul>
       <li><a href="{{ path('path') }}">relative path</a></li>
       <li><a href="{{ url('path') }}">absolute path</a></li>
       <li><img src="{{ asset('images/logo.png') }}" alt="symfony img logo" /></li>
        <a href="{{absolute_url(asset('images/logo.png')) }}" />{{absolute_url(asset('images/logo.png')) }}</a>
    </ul>

</div>
{% endblock %}
{% block javascripts %}
    <script>
        var route="{{ string|escape('js') }}" ;
        alert(route);
    </script>
{% endblock %}
